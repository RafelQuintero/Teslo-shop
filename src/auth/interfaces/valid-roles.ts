//en vez de se una interface digamosle que sea una numeracion para este métodos.

export enum ValiRoles {
  //recuede que debe ser un arreglo de setring

  admin = 'admin',
  superUser = 'super-user',
  user = 'user',
}
